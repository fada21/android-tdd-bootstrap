<resources>

    <!-- Base application theme. -->
    <style name="Theme.TabTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="actionBarStyle">@style/TabActionBarStyle</item>
        <item name="searchViewStyle">@style/TabSearchViewStyle</item>

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

    </style>

    <style name="TabActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/accent</item>
        <!-- style when needed -->
    </style>

    <style name="TabSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- style when needed -->
    </style>

</resources>
